"""常駐モード（サーバーモード）。

エージェントから見えるインターフェースは通常の CLI のままで、内部でのみ
常駐プロセスを使う。詳細は docs/10_server_mode_plan.md を参照。

このパッケージの ``__init__`` は意図的に空にしてある。薄いクライアントが
``pyselector.server.client`` だけを読み込めるようにし、pywin32 に依存する
``pipe`` を巻き込まないため。
"""
